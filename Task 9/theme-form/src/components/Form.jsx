import Fields from "./Fields";
import Button from "./Button";

const Form = () => {
  return (
    <div className="form-container">
      <Fields />
      <Button />
    </div>
  );
};

export default Form;
